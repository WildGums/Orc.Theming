<catel:UserControl x:Class="Orc.Theming.Views.BaseColorSchemeSwitcherWithIconView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://schemas.catelproject.com">

    <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Label VerticalAlignment="Center" Content="{catel:LanguageBinding BaseColorSchemeSwitcherView_SelectBaseColorScheme}" />
        <ListView ItemsSource="{Binding BaseColorSchemeUris}" SelectedItem="{Binding SelectedBaseColorSchemeUri}" >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Image Source="{Binding Value}" VerticalAlignment="Center" />
                        <Label Content="{Binding Key}" VerticalAlignment="Center" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>

</catel:UserControl>
